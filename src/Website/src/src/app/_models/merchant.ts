import {Pos} from './pos';

export class Merchant {
  id: string;
  name: string;
  fiscalCode: string;
  address: string;
  zipCode: string;
  city: string;
  url: string;
  pos: Pos[];
}

